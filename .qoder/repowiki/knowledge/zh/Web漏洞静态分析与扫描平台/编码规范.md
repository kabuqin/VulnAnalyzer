- 所有分析函数（如`analyze_xss`）均返回`list[Finding]`对象列表，并在主入口`analyze()`中统一聚合与去重。
- 漏洞发现必须包含结构化元数据，包括`risk`（风险等级）、`confidence`（置信度）、`cwe`（通用弱点枚举）及`verification`（验证建议）。
- 在`vuln_analyzer.py`中广泛使用预编译的正则表达式模式（如`SECRET_PATTERNS`）进行静态特征匹配，以提高扫描效率。